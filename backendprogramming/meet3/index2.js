const{functest, divide} = require('./utils'); //if function used is more than 1 use {}, but if its not u can just use a name
//const divide = require('./utils') -> this is fine

console.log(functest(4));
console.log(divide(6, 3));